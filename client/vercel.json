{
    "builds": [
      {
        "src": "client/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "engines": {
      "node": "18.x"
    },
    "framework": "vite",
    "installCommand": "cd client && ./debug.sh",
    "buildCommand": "cd client && yarn build"
  }