{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "engines": {
      "node": "18.x"
    },
    "framework": "vite",
    "installCommand": "yarn install && yarn list vite || echo 'vite not found'",
    "buildCommand": "./build.sh"
  }